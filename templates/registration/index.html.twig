{% extends 'base.html.twig' %}

{% block body %}

    <div class="container">
        <div class="mt-3">
            <h3>Create an account</h3>
        </div>
        <div class="mt-3">
            {{ form(registerForm) }}
        </div>
    </div>

{% endblock %}
