{% extends 'base.html.twig' %}

{% block title %}Cities{% endblock %}

{% block body %}
    <br>
    <div class="container">
        <h5>Cities</h5>
        <br>
        <table class="table">
            <tbody>
            <tr>
                <th>Id</th>
                <td>{{ city.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ city.name }}</td>
            </tr>
            <tr>
                <th>Telephone</th>
                <td>{{ city.telephone }}</td>
            </tr>
            </tbody>
        </table>

        <a href="{{ path('list_of_cities_index') }}">back to list</a>

        <a href="{{ path('list_of_cities_edit', {'id': city.id}) }}">edit</a>

        {{ include('list_of_cities/_delete_form.html.twig') }}
    </div>

{% endblock %}
